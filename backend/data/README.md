# This is the data directory for the backend
# It will contain:
# - reviews.jsonl (review metadata)
# - spfresh_index/ (SPFresh vector index files)

This directory is mounted as a volume in Docker to persist data.
